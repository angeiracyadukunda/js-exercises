function celsiusToFahrenheit(celsius) {
    // Use the conversion formula
    return (celsius * 9/5) + 32;
}

// Example usage:
const celsiusValue = 25;

console.log(`${celsiusValue} degrees Celsius is equivalent to ${celsiusToFahrenheit(celsiusValue)} degrees Fahrenheit.`);
