


function calculateRectangleArea(length, width) {
    // Use the formula for the area of a rectangle
    return length * width;
}

// Example usage:
const rectangleLength = 5;
const rectangleWidth = 3;

const areaResult = calculateRectangleArea(rectangleLength, rectangleWidth);
console.log(`Area of the rectangle: ${areaResult}`);
