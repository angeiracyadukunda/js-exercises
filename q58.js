



function calculatePower(base, exponent) {
    // Use the ** operator for exponentiation
    return base ** exponent;
}

// Example usage:
const base = 2;
const exponent = 3;

console.log(`${base}^${exponent} is:`, calculatePower(base, exponent)); // Output: 8
