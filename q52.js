

function squareRoot(number) {
    return Math.sqrt(number);
}

// Example usage:
const originalNumber = 25;
const sqrtResult = squareRoot(originalNumber);

console.log(`Square root of ${originalNumber}:`, sqrtResult);
